{
  "name": "to-be-read-exchange-hub",
  "version": "1.0.0",
  "description": "Smart inventory management system for book exchange",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "build": "node -e \"console.log('build placeholder'); const fs = require('fs'); const path = require('path'); fs.mkdirSync('dist', { recursive: true }); fs.cpSync('src', 'dist/src', { recursive: true }); fs.copyFileSync('package.json', 'dist/package.json'); console.log('✓ Created dist/ directory with src/ and package.json');\"",
    "lint": "node -e \"console.log('lint placeholder')\"",
    "format:check": "node -e \"console.log('format check placeholder')\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.12.2",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express-validator": "^7.2.1",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "pg": "^8.16.3",
    "winston": "^3.18.3"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4"
  }
}
